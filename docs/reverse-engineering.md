# Reverse Engineering

Investigation records live in [evidence/](evidence/INDEX.md), one file per
record (`evidence/re/RE-NNN.md`). Search the index by ID or topic tag; do not
bulk-read the corpus.

To add a record, create the next unused `RE-NNN.md` in the existing shape,
then run `python3 tools/docs/gen_evidence_index.py`.
