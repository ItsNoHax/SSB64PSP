# RE-282 — Physical-hardware confirmation of RE-281's `Ci4`/`PsmT4` fix (Mario)

Status: COMPLETE
Topics: texture, fighter, hardware, visual-regression
Relevant files: crates/ssb-rom/src/psp_texture.rs (unchanged this session),
psp/target/mipsel-sony-psp/release/ssb64-psp.prx, assets/generated/ssb64.pak

**Question.** RE-281 fixed RE-272's Mario-face `Ci4`/`PsmT4` nibble-order
artifact and confirmed it on PPSSPP software rendering only. RE-281's own
Confidence note and `STATUS.md`'s required next action name one remaining
item: a physical-hardware recapture proving the fix holds outside PPSSPP's
software rasterizer, not just via the "PPSSPP and hardware reproduce the same
bug identically" precedent. PSP-1000 coverage remains separately deferred
(no unit available) and is out of scope for this record.

**Hardware.** PSP Slim (the same unit RE-201–RE-271 used), firmware 6.61,
ARK/Infinity CFW, PSPLink v3.2.1, connected via `054c:01c9` USB. (The PSP-3000
unit RE-273 used was not exercised this session; not required since RE-273
already established Mario renders correctly cross-unit pre-fix, and this
record's question is specifically about the post-fix nibble order, which is
pack/GE-read behavior independent of which physical unit reads it.)

**Build.** `cargo psp --release --features regression_capture_mario` against
current `main` (RE-281's applied fix, no production-code change this
session). PRX SHA-256
`dacdd5907d743fea5b478b73eb67018b2f3f01a4f0182d33bb257b45dc04219a`. Loaded via
`host0:` (`usbhostfs_pc` + `ldstart`), against `assets/generated/ssb64.pak`
(RE-281's rebuilt pack, hash
`0065c65d92cf805b12f6157259e0e0da9a6ab0f872c01d3aae271c49e277a368`, confirmed
via the `ssb64.pak` symlink target before load). `reset` issued before this
load (no prior module state to worry about, applied as the default per this
project's own PSPLink convention).

**Capture.** After the 240-tick deterministic-capture freeze, `exlist` empty
and `main_thread` alive (`thlist`) — zero exceptions. Native `scrshot` BMP
capture, SHA-256
`5e72668e314e3a3f25ec8711731e0a8c58312b7d3b9ea575b1216413fcd417e0` (not
committed, kept out of Git per policy).

**Visual confirmation (RE-272's original complaint).** The cap's white "M"
emblem renders as a clean, correctly-rounded logo with no repeating
adjacent-texel-pair-swap artifact — the same defect class RE-272 originally
found and RE-281 fixed on PPSSPP. Visually indistinguishable from RE-281's
post-fix PPSSPP golden in the affected region.

**Quantitative comparison.** Hardware capture (480x272) diffed against
`tests/golden/r2-mario-fighter.png` (960x544, PPSSPPHeadless's native
capture resolution) after box-downsampling the golden to the hardware's
resolution and excluding the PSPLink diagnostic-overlay region (top-left
text, per this skill's own overlay-exclusion rule): 9,917/130,560 pixels
(7.6%) show any difference, but only 349 (0.27%) exceed a 30-level
channel-sum threshold, with a maximum channel-sum difference of 618/765.
This is the same order of divergence this project's existing hardware-vs-
PPSSPP captures already show and already attribute to real-GE-vs-software-
rasterizer antialiasing differences (RE-203, RE-207 "matches ... modulo
expected edge antialiasing"), not a new defect — no diff clusters on the cap
emblem specifically, which is the region RE-272/280/281 concern.

**Conclusion.** The `Ci4`/`PsmT4` nibble-order fix holds on real PSP
hardware, not only PPSSPP's software rasterizer. This closes the one
remaining item RE-281's Confidence note named. `STATUS.md`'s physical R2
matrix now has PSP-1000 as its sole remaining, deliberately-deferred gap
(access-blocked, not a correctness question).

**Confidence: high.** Zero exceptions, clean visual match on the exact named
complaint region, and a quantitative diff consistent with this project's own
established hardware/PPSSPP noise floor rather than a new artifact.
